# ProfMatch API Configuration
# Copy this to .env.development or .env.production and fill in values.
# Pydantic loads files in order: .env.production, .env.development, .env
# Only the first file found is used.

# Environment (development | production)
ENV=development

# Debug mode (set to true for development)
DEBUG=false

# Redis connection URL
REDIS_URL=redis://localhost:6379

# PostgreSQL connection URL
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/profmatch

# CORS allowed origins (comma-separated for multiple)
CORS_ORIGINS=["http://localhost:3000"]

# File upload settings
MAX_UPLOAD_SIZE_MB=10

# Session TTL in hours
SESSION_TTL_HOURS=24

# Google Gemini API key (required for LLM features)
GEMINI_API_KEY=your_gemini_api_key_here

# Tavily API key (for web search features)
TAVILY_API_KEY=your_tavily_api_key_here

# Google Cloud Storage settings
GCS_BUCKET_NAME=your_bucket_name_here
GCS_PROJECT_ID=your_project_id_here
